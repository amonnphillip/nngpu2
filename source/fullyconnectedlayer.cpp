#include "fullyconnectedlayer.h"

FullyConnectedLayer::FullyConnectedLayer(int dimentionx, int dimentiony, int dimentionz, int forwardx, int forwardy, int forwardz, INNetworkLayer* previousLayer)
{
	/*
	nodeCount = dimentionx * dimentiony * dimentionz; THIS IS THE SAME AS FORWARD!!!
	forwardCount = forwardx * forwardy * forwardz;
	Layer::Initialize(
		"fullyconnected",
		forwardCount,
		previousLayer->GetForwardNodeCount(),
		nodeCount,
		nodeCount,
		true);*/
}
